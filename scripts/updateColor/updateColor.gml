//@description updateColor(color)
//@param color

var color = argument0;

var valor_actual = obj_controller.activeColors[? color];

show_debug_message(string(color));
show_debug_message(string(valor_actual));

return valor_actual;
